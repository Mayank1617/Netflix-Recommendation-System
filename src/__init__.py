"""Netflix Prize recommendation system — source modules.

Components:
    data       Data Processing Pipeline (parse, subset, temporal split, indexing)
    models     Model Training Pipeline (baseline, ItemCF, MF, NCF)
    evaluate   Evaluation Scripts (RMSE, MAP@10, ranking metrics)
    recommend  Recommendation Generation Module (Top-K + explanations)
"""
